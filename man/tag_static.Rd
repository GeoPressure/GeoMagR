% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geomag_calib.R
\name{tag_static}
\alias{tag_static}
\title{Classify Static and Moving Samples From Acceleration}
\usage{
tag_static(
  tag,
  static_thr_hard = NULL,
  static_thr_outlier = NULL,
  use_activity = FALSE
)
}
\arguments{
\item{tag}{A \code{GeoPressureR} tag object containing a \verb{$magnetic} data frame
with \code{acceleration_x}, \code{acceleration_y}, and \code{acceleration_z}. If
\code{tag$acceleration} is present, its \code{value} column is used as an activity
signal.}

\item{static_thr_hard}{Numeric or \code{NULL}. Hard threshold around 1 g used to
build the initial candidate static set. If \code{NULL}, the default \code{0.15} is
used.}

\item{static_thr_outlier}{Numeric or \code{NULL}. MAD threshold used to remove
outliers among the candidate static samples. If \code{NULL}, the default \code{3} is
used.}

\item{use_activity}{Logical. If \code{TRUE} and \code{tag$acceleration} is available,
use activity clustering to flag high-activity samples as moving before MAD
outlier filtering.}
}
\value{
The input tag object with \code{tag$magnetic$is_static} added or updated.
\code{tag$magnetic$is_static} is a logical vector where \code{TRUE} denotes samples
considered static and \code{FALSE} denotes samples considered moving.
}
\description{
Computes a logical \code{is_static} flag for each row in \code{tag$magnetic} using the
acceleration magnitude.

The classification is done in three steps:
\enumerate{
\item Compute the acceleration norm \code{gn = sqrt(x^2 + y^2 + z^2)} for each sample.
\item Apply the hard threshold \code{abs(gn - 1) < static_thr_hard} to build the
initial static candidates.
\item If \code{tag$acceleration} is available, cluster the positive \code{value} samples
into low- and high-activity groups, force the high-activity group to
\code{FALSE}, then remove outliers among the remaining candidate static points
using a MAD rule with threshold \code{static_thr_outlier}.
}
}
\examples{
library(GeoPressureR)
withr::with_dir(system.file("extdata", package = "GeoMagR"), {
  tag <- tag_create("14DM", quiet = TRUE)
  tag_static_obj <- tag_static(tag)
  table(tag_static_obj$magnetic$is_static)

  # A stricter hard threshold keeps fewer samples as static
  tag_static_strict <- tag_static(tag, static_thr_hard = 0.05, static_thr_outlier = 2)
  table(tag_static_strict$magnetic$is_static)
})

}
